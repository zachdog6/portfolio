Basic message app to demostrate Java, Spring Boot, Spring JDBC, and JavaFX. 

The application will ask for the URL to your computer: http://your_ip_address:port (Default port is 8080). This was done as a simple way to send messages via API calls without having a dedicated server.